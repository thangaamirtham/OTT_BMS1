import { BluebackgroundDirective } from './bluebackground.directive';

describe('BluebackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new BluebackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
